---
import Page from "../../../pages_template/machinery/index.astro";
export function getStaticPaths() {
    return ["en", "zh", "th"].map((lang) => ({
        params: { lang },
    }));
}

const { lang } = Astro.params;
---

<Page language={lang} />
